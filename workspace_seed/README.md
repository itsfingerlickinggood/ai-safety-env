# Research workspace

Start with `task.md`, then read `paper_redacted.md` and `RESEARCH_PROTOCOL.md`. The `public/` datasets and executable artifacts are materialized fresh when the HUD environment initializes.

All files you create must remain in this workspace. The grader evaluates the final workspace only after you submit.
